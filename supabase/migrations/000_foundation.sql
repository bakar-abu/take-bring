-- Take-Bring: foundation note
-- Run future table SQL from Backend tickets in the Supabase SQL Editor
-- (or via supabase/migrations when CLI is added).
--
-- Project is connected from Next.js using:
--   NEXT_PUBLIC_SUPABASE_URL
--   NEXT_PUBLIC_SUPABASE_ANON_KEY
--   SUPABASE_SERVICE_ROLE_KEY (server only)
--
-- No tables yet — first schema lands with [Backend] Leads.

select 1 as supabase_ready;
